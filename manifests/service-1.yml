apiVersion: v1
kind: Service
metadata:
    name: "atom-c6a3"
    labels:
        app: "atom-c6a3"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "atom-c6a3"