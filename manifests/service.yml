apiVersion: v1
kind: Service
metadata:
  name: "atom-7c25"
  labels:
    app: "atom-7c25"
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: "atom-7c25"
